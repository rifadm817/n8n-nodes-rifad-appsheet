import { ExecutionBaseError } from './abstract/execution-base.error';
import type { INode } from '../Interfaces';
export declare class CredentialAccessError extends ExecutionBaseError {
    readonly node: INode;
    readonly description = "Please recreate the credential or ask its owner to share it with you.";
    readonly level = "warning";
    constructor(node: INode, credentialId: string, workflowId: string);
}
